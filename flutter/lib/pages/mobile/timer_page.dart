import 'package:flutter/material.dart';

class MobileTimerPage extends StatelessWidget {
  const MobileTimerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Timer page'));
  }
}
