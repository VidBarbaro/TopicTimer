import 'package:flutter/material.dart';

class MobilePersonalPage extends StatelessWidget {
  const MobilePersonalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Personal page'));
  }
}
