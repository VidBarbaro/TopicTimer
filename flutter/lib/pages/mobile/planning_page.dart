import 'package:flutter/material.dart';

class MobilePlanningPage extends StatelessWidget {
  const MobilePlanningPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Planning page'));
  }
}
