import 'dart:ui';

class BarData {
  const BarData(this.color, this.value, this.shadowValue, this.subject);
  final Color color;
  final double value;
  final double shadowValue;
  final String subject;
}