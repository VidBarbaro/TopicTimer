import 'package:flutter/material.dart';

class PlanningCalendarComp extends StatelessWidget {
  PlanningCalendarComp({super.key});

@override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Planning Calendar'),
        );
  }
}