import 'package:flutter/material.dart';

class TrackedTimeComp extends StatelessWidget {
  TrackedTimeComp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      children: [Text('first item')],
    );
  }
}
